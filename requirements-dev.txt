# ── 開発・テスト依存パッケージ ─────────────────────────────────
-r requirements.txt
pytest>=9.1.1
pytest-cov>=7.1.0
pytest-asyncio>=0.24.0
httpx>=0.28.1
ruff>=0.16.6
mypy>=2.3.1
types-psycopg2>=2.9.21.20241019
pip-audit>=2.10.1

# ── セキュリティピン留め（間接依存の脆弱性対策） ──────────────────
Pygments>=2.20.0
# pip-audit の間接依存（pip-api が pip をバージョン指定なしで要求）経由で
# OSV-Scanner が脆弱な古いバージョンとして解決してしまうため明示的に固定する
# （PYSEC-2020-173 等、pip<26.2 の複数CVE。Issue #63対応時に発覚）
pip>=26.2.1
filelock>=3.32.6
